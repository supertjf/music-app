// (function flexible(d, b) {
//   var h = b.documentElement;
//   var c = d.devicePixelRatio || 1;
//   function g() {
//     if (b.body) {
//       b.body.style.fontSize = 12 * c + "px";
//       setTimeout(function() {
//         b.querySelector("body").style.display = "block";
//       }, 20);
//     } else {
//       b.addEventListener("DOMContentLoaded", g);
//     }
//   }
//   g();
//   function a() {
//     var i = h.clientWidth / 10;
//     h.style.fontSize = i + "px";
//     console.log(h.style.fontSize);
//   }
//   a();
//   d.addEventListener("resize", a);
//   d.addEventListener("pageshow", function(i) {
//     if (i.persisted) {
//       a();
//     }
//   });
//   if (c >= 2) {
//     var f = b.createElement("body");
//     var e = b.createElement("div");
//     e.style.border = ".5px solid transparent";
//     f.appendChild(e);
//     h.appendChild(f);
//     if (e.offsetHeight === 1) {
//       h.classList.add("hairlines");
//     }
//     h.removeChild(f);
//   }
// })(window, document);

(function flexible(window, document) {
  var docEl = document.documentElement
  var dpr = window.devicePixelRatio || 1

  // adjust body font size
  function setBodyFontSize() {
    if (document.body) {
      document.body.style.fontSize = (12 * dpr) + 'px'
    }
    else {
      document.addEventListener('DOMContentLoaded', setBodyFontSize)
    }
  }
  setBodyFontSize();

  // set 1rem = viewWidth / 10
  function setRemUnit() {
    var rem = docEl.clientWidth / 7.5
    if (rem >= 100) {
      rem = 100
    } else if (rem <= 42.6) {
      rem = 42.6
    };
    docEl.style.fontSize = rem + 'px'
  }

  setRemUnit()

  // reset rem unit on page resize
  window.addEventListener('resize', setRemUnit)
  window.addEventListener('pageshow', function (e) {
    if (e.persisted) {
      setRemUnit()
    }
  })

  // detect 0.5px supports
  if (dpr >= 2) {
    var fakeBody = document.createElement('body')
    var testElement = document.createElement('div')
    testElement.style.border = '.5px solid transparent'
    fakeBody.appendChild(testElement)
    docEl.appendChild(fakeBody)
    if (testElement.offsetHeight === 1) {
      docEl.classList.add('hairlines')
    }
    docEl.removeChild(fakeBody)
  }
}(window, document))
